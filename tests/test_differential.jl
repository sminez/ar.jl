# Test cases for differnetial.jl

println("-----------------------------------------------------------------")
facts("partial derivatives") do
    context("") do
    #=
    0:3 by themselves
    negative based on metric
    Dμ and del_i being built correctly
    changing div by/into ?
    =#
    end
end
